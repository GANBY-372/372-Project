@startuml
left to right direction
skinparam packageStyle rectangle
:Customer:
:Dealer:
package ReturnVehicleRequest #AliceBlue {
	Customer --> (Submit return) #green;line.bold;text:green
	Customer --> (Cancel return) #green;line.bold;text:green
	Customer --> (View return receipt) #green;line.bold;text:green
	Customer --> (View return history) #green;line.bold;text:green
	Customer --> (View return status) #green;line.bold;text:green
	Customer --> (Login) #green;line.bold;text:green
	Customer --> (Logout) #green;line.bold;text:green

	(Submit return) <-- Dealer #red;line.bold;text:red
	(Cancel return) <-- Dealer #red;line.bold;text:red
	(View return receipt) <-- Dealer #red;line.bold;text:red
	(View return history) <-- Dealer #red;line.bold;text:red
	(View return status) <-- Dealer #red;line.bold;text:red
	(Login) <-- Dealer #red;line.bold;text:red
	(Logout) <-- Dealer #red;line.bold;text:red

	rectangle "Dealer-Exclusive Actions" #lightblue {
		(Process Return) as PR #lightpink
		PR <-- Dealer #red;line.bold;text:red

		(Add returned vehicle to rental pool) as (ARVTRP) #lightpink
		ARVTRP <-- Dealer #red;line.bold;text:red

		(View return all return receipts) as VARR #lightpink
		VARR <-- Dealer #red;line.bold;text:red

		(View all return histories) as VARH #lightpink
		VARH <-- Dealer #red;line.bold;text:red

		(View all return statuses) as VARS #lightpink
		VARS <-- Dealer #red;line.bold;text:red
	}
}
@enduml
